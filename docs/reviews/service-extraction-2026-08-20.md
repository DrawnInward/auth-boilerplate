# Review — controllers hold no transactions (2026-08-20)

Scope: the working tree after extracting every inline controller transaction into
services (`credential.service`, `organization.service`, `invitationService.inviteAdmin`).
`/code-review high`, 11 candidates → 10 findings (7 CONFIRMED, 3 PLAUSIBLE), 1 refuted
(`inviteAdmin` casing: `invalidatePendingInvitations` and `getUser` both lower-case).
Gates before and after: 47 suites, 1,054 existing tests untouched.

| #   | Finding                                                                                                                                        | Verdict   | Outcome                                                                                                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `resetPassword` acquires a second pool connection (`getUser`) while holding the txn client + invitation FOR UPDATE, then hashes with both held | CONFIRMED | Fixed: pre-validate + `getUser` + hash on the pool, txn re-validates under lock and writes                                                                                                                                              |
| 2   | `changePassword`/`setPassword` open the txn first, then pool-read `getUserWithPasswordById` and run bcrypt on the held client                  | CONFIRMED | Fixed: read, guards, verify, hash before `runTransaction`; txn wraps only the writes                                                                                                                                                    |
| 3   | `completeRegistration`/`completeAdminRegistration` hash inside the txn after FOR UPDATE                                                        | CONFIRMED | Fixed: pool pre-validate (refuses bad tokens before any bcrypt), hash, then locked re-validate + writes                                                                                                                                 |
| 4   | `confirmEmailChange` availability check on the pool while holding the client; not serialised with the write                                    | PLAUSIBLE | Fixed the acquire-while-holding: `getUser` gained an optional client param. The two-claimants race still lands on the `users.email` unique index (409) — accepted                                                                       |
| 5   | `disableUserMfa` duplicates `mfa.service`'s disable pair; header claim "reads stay on the pool" false for this flow                            | CONFIRMED | Header rewritten. Duplication deferred: folding into `mfa.service` moves the email inside the service; the integration spec spies `services.email.sendMfaDisabled`, which a composition-time method reference bypasses                  |
| 6   | `inviteAdmin` is a second copy of `inviteMember`'s supersede+mint; admin-exists 409 stayed in the controller                                   | PLAUSIBLE | Shared private `mintInvitation`. The 409 stays in the controller: moving it needs an `admins.getAdmin` dep, i.e. one fake added to the existing `invitationService.test` builder — deferred so this lands with zero existing-test edits |
| 7   | `CompleteRegistrationInput`/`TokenAndPassword` re-type shared Zod contracts                                                                    | CONFIRMED | Fixed: signatures use `CompleteRegistrationDto`/`ResetPasswordDto` from shared                                                                                                                                                          |
| 8   | `revokeTokens` ad-hoc structural type disagrees with `account.service` and the model; `runTransaction` literal pasted a 6th time               | CONFIRMED | Fixed: `typeof revokeUserTokens`; `RunTransaction` exported from `utils/withTransaction` and used by the new services (older services unchanged)                                                                                        |
| 9   | Inline `bcrypt.compare` lambda in the composition root — ninth direct call site, no util                                                       | CONFIRMED | Fixed: `verifyPassword` in `utils/hashPassword.ts`, injected. Other eight sites left for a separate sweep                                                                                                                               |
| 10  | `owner_id is required` guard widens `ownerId` to `string \| undefined` for a case the route schema and model already refuse                    | PLAUSIBLE | Fixed: `ownerId: string`, guard and its unit case removed                                                                                                                                                                               |

Process: the review agent stalled after three verifier verdicts; one nudge collected the
remaining eight. Dropped outright by the reviewer: per-test bcrypt hashing in the new
`adminUsers` spec block (`jest.env.ts` pins cost 4).
